language: python
python:
    - "3.6"

before_install:
    - sudo apt-get install gfortran
    - ./install-suitesparse.sh
    - ./install-cvode3.1.sh
    - ./install-chiantipy.sh
#    - ./install-grackle.sh
# command to install dependencies
install:
    - pip install -r requirements.txt
    - pip install -e .
# command to run tests
script:
    - py.test


